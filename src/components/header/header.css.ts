import {style} from 'styled-vanilla-extract/qwik';

export const container=style({
    gridArea: 'header',
    backgroundColor: 'red',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    justifyItems: 'center',
    height: 60
});
